Create a pop-up window for errors and debugging.

Make MAGIC_MAX auto-update.

XP gain needs balancing.

savefile should be converted to an .ini file, which should make
    future edits and reorganizing it easier.
    
a default savefile should be created if one isn't found.

Content and balancing! Very low priority for now.

Item descs wrap, but only on a letter by letter basis.
    It would be preferable to wrap whole words.

Status effects that temp reduce stats. STR,DEF,MAG...?

Use game version to check savefile version.
